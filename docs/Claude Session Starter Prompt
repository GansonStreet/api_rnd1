# Ganson — Claude Session Starter Prompt
## Copy and paste this at the start of every new Claude session

---

```
I'm building a Node.js app. My API keys are stored in a central key store called Ganson.

## Ganson Server
URL:      https://admin.gansonstreet.com
Password: (your password)

## My .env file contains ONLY these 2 lines — never add real API keys to .env:
GANSON_URL=https://admin.gansonstreet.com
GANSON_PASS=YourPassword

## Available clients and their keys:
- mario  → ANTHROPIC_API_KEY, TWILIO_SID, TWILIO_TOKEN, TWILIO_FROM, GITHUB_TOKEN
- bnmc   → ANTHROPIC_API_KEY, SUPABASE_URL, SUPABASE_KEY
- ganson → ANTHROPIC_API_KEY, GITHUB_TOKEN, SUPABASE_URL, SUPABASE_KEY

## Rules you must follow:
1. NEVER hardcode any API key anywhere in the code
2. ALWAYS place ganson.js in the project root (code below)
3. ALWAYS call getCredentials('clientname') explicitly — client name is always required
4. ALWAYS wrap top-level credential loading in an async IIFE or async function
5. ALWAYS use require('dotenv').config() at the top of every file

## How to load credentials — always do it this way:
const { getCredentials } = require('./ganson');

(async () => {
  // Load ALL keys for a client
  const creds = await getCredentials('mario');

  // Then use keys like this:
  creds.ANTHROPIC_API_KEY
  creds.TWILIO_SID
  creds.TWILIO_TOKEN
  creds.TWILIO_FROM
  creds.SUPABASE_URL
  creds.SUPABASE_KEY

  // Load keys for ONE service only (optional)
  const twilioKeys = await getServiceCredentials('twilio', 'mario');

  // rest of app goes here...
})();

## ganson.js — place this file in every project root, never modify it:
require('dotenv').config();

const GANSON_URL  = process.env.GANSON_URL;
const GANSON_PASS = process.env.GANSON_PASS;

const getToken = () =>
  Buffer.from(GANSON_PASS + ':gansonstreet').toString('base64');

// Fetch ALL credentials for a client
const getCredentials = async (client, env = null) => {
  const url = new URL(`${GANSON_URL}/api/credentials/${client}`);
  if (env) url.searchParams.set('env', env);
  const res = await fetch(url.toString(), {
    headers: { Authorization: 'Bearer ' + getToken() }
  });
  if (!res.ok) throw new Error(`Ganson error: ${res.status} ${res.statusText}`);
  const data = await res.json();
  return data.credentials;
};

// Fetch credentials for ONE service only
const getServiceCredentials = async (service, client) => {
  const res = await fetch(
    `${GANSON_URL}/api/credentials/${client}/${service}`,
    { headers: { Authorization: 'Bearer ' + getToken() } }
  );
  if (!res.ok) throw new Error(`Ganson error: ${res.status} ${res.statusText}`);
  const data = await res.json();
  return data.credentials;
};

module.exports = { getCredentials, getServiceCredentials };
```

---

## Example — tell Claude what you're building like this:

> "Build me an Express app that sends SMS via Twilio.
> Use getCredentials('mario') to load keys from Ganson.
> Never hardcode any API keys."

---

## Client quick reference:
| Project | Client Slug | Keys Available |
|---------|-------------|----------------|
| Marios Bistro & Brews | `mario` | Anthropic, Twilio, GitHub |
| BNMC Parking | `bnmc` | Anthropic, Supabase |
| Ganson | `ganson` | Anthropic, GitHub, Supabase |
